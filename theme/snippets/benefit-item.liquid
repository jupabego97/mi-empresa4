<div class="nt-benefit">
  <div class="nt-benefit__icon">
    {% render 'icon', name: icon | default: 'shield', size: 24 %}
  </div>
  <h3 class="font-semibold text-ink mb-1">{{ title }}</h3>
  <p class="text-sm text-ink-muted">{{ text }}</p>
</div>
